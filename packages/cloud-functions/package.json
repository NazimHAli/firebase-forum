{
  "name": "@firebase-forum/cloud-functions",
  "version": "1.0.0",
  "main": "../../dist/cloud-functions/index.js",
  "scripts": {
    "build": "yarn build:test && tsc",
    "build:test": "yarn lint && yarn test",
    "cm": "cz",
    "lint": "prettier --check .",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "*",
    "@types/node": "*",
    "@typescript-eslint/eslint-plugin": "*",
    "@typescript-eslint/parser": "*",
    "eslint": "*",
    "eslint-config-prettier": "*",
    "jest": "*",
    "prettier": "*",
    "ts-jest": "*",
    "ts-node": "*",
    "tsconfig-paths": "*",
    "typescript": "*"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "firebase-admin": "^10.0.0",
    "firebase-functions": "^3.16.0"
  }
}
